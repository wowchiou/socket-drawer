import React from 'react';
import COMPONENT_NAME from './COMPONENT_NAME';

export default {
  title: 'SCOPE_NAME/COMPONENT_NAME',
  component: COMPONENT_NAME,
};

const Template = (args) => <COMPONENT_NAME {...args} />;

export const primary = Template.bind({});
primary.args = {};
